{
  "name": "ts-merge-sorted-collections",
  "version": "1.0.0",
  "scripts": {
    "start": "tsx src/merge.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
